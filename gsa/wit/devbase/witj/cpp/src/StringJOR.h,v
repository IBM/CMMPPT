head	1.3;
access;
symbols;
locks; strict;
comment	@ * @;


1.3
date	2011.04.15.15.49.06;	author rjw;	state Exp;
branches;
next	1.2;

1.2
date	2011.04.14.16.11.43;	author rjw;	state Exp;
branches;
next	1.1;

1.1
date	2011.04.12.00.33.14;	author rjw;	state Exp;
branches;
next	;


desc
@@


1.3
log
@Class StringJOR
@
text
@//------------------------------------------------------------------------------
// WIT-J C++ Header File StringJOR.h.
//
// Contains the declaration of class StringJOR.
//------------------------------------------------------------------------------

#ifndef StringJOR_h
#define StringJOR_h

#include <JniClient.h>

//------------------------------------------------------------------------------
// Class StringJOR
//
// "String Java Object Representation"
// Represents a Java String.
//
// An instance of this class stores a local reference to the represented Java
// String. Thus an instance of this class should always be destroyed by the time
// the native method in which it was created returns.
//
// Class Hierarchy:
//
// JniClient
//    StringJOR
//------------------------------------------------------------------------------

namespace WitJ
{
class StringJOR: public JniClient
   {
   //---------------------------------------------------------------------------
   // Public member functions.
   //---------------------------------------------------------------------------

   public:

   explicit StringJOR (_jstring * theJString, JNIEnv *);
      //
      // Constructor.
      // theJString must be a local reference.

   explicit StringJOR (JNIEnv *);
      //
      // Constructor.
      // The represented Java String will be the empty string.

   ~StringJOR ();
      //
      // Destructor.

   void makeJavaString (const char * theCString);
      //
      // Creates a new Java String that's a copy of theCString.
      // The new Java String becomes the represented Java String.

   inline _jstring * getJString ()
      {
      return myJString_;
      }

   const char * getCString ();
      //
      // Returns the represented Java String as a c-string.

   //---------------------------------------------------------------------------
   // Private member functions.
   //---------------------------------------------------------------------------

   private:

   StringJOR              (const StringJOR &);
   StringJOR & operator = (const StringJOR &);
      //
      // Not implemented; prevents accidental copying and assignment.

   //---------------------------------------------------------------------------
   // Private member data.
   //---------------------------------------------------------------------------

   _jstring * myJString_;
      //          
      // A local reference to the represented Java String.

   const char * myCString_;
      //
      // The represented Java String as a c-string or NULL.
   };
};
//
// End of namespace WitJ.

#endif
@


1.2
log
@Class StringJOR
@
text
@a61 5
   inline operator const char * ()
      {
      return getCString ();
      }

@


1.1
log
@Class StringJOR
@
text
@d62 5
@

